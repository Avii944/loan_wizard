# Groq API key — get a free key from https://console.groq.com/keys
# If left empty, the system uses its built-in deterministic fallback engine
GROQ_API_KEY=

# Flask secret (any random string)
FLASK_SECRET=loan-wizard-dev-secret-change-me

# Audit DB path
AUDIT_DB=audit_repo/loan_wizard.db
